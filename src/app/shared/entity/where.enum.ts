export enum WHERE {
    back,           // 返回上一页
    login,
    home,               // Reporter = 1, 默认情况下，从0开始为元素编号。 也可赋值，后续值递增
    resetPassword,
    
    
    Developer,
    Maintainer,
    Owner,
    Guest
    
}
