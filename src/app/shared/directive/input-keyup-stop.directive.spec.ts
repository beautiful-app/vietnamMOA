import { InputKeyupStopDirective } from './input-keyup-stop.directive';

describe('InputKeyupStopDirective', () => {
  it('should create an instance', () => {
    const directive = new InputKeyupStopDirective();
    expect(directive).toBeTruthy();
  });
});
